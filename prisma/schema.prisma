// Pura Esencia - Prisma schema (MODO LOCAL VALIDACION)
// Cambiado a SQLite para validar el catalogo sin servidores externos ($0).
// Para produccion en la nube, volver a postgresql + DATABASE_URL (Neon/Supabase).

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Restaurant {
  id        String   @id @default(cuid())
  name      String   @default("Pura Esencia")
  phone     String   @default("")
  whatsapp  String   @default("")
  logo      String   @default("")
  password  String   @default("admin123")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Category {
  id        String     @id @default(cuid())
  name      String
  image     String     @default("")
  sortOrder Int        @default(0)
  isHidden  Boolean    @default(false)
  menuItems MenuItem[]
  createdAt DateTime   @default(now())
  updatedAt DateTime   @updatedAt

  @@index([sortOrder])
}

model MenuItem {
  id          String   @id @default(cuid())
  name        String
  price       Int      @default(0)
  description String   @default("")
  images      String   @default("[]")
  likes       Int      @default(0)
  isFeatured  Boolean  @default(false)
  isHidden    Boolean  @default(false)
  categoryId  String
  category    Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@index([categoryId])
  @@index([isFeatured])
  @@index([isHidden])
  @@index([likes])
}
